<li class="list-kitchen"  id="{{name}}">
    <a href="#" onclick="settings.add_kitchen($(this).parent().attr('id')); settings.delete_kitchen_invitation($(this).parent().attr('id'))" style="float: right">
        <span class="glyphicon glyphicon-ok-sign" id="kitchenInvitation-{{name}}" style="color:#008000"></span>
    </a>
    {{name}}
    <a href="#" onclick="settings.delete_kitchen_invitation($(this).parent().attr('id'))" style="float: right">
        <span class="glyphicon glyphicon-remove glyphicon-user" id="settingsDelete-{{name}}" style="color:#a94442"></span>
    </a>
</li>
<hr/>